/**
 * @property classList 配列で定義したクラス名
 * @property textContents ボタンテキスト
 */
type ButtonProps = {
  classList: string[];
  textContents: string;
};


/**
 * @description plane buttonの作成に必要
 * @property classList 配列で定義したクラス名
 * @property textContents ボタンテキスト
 */
export type PlaneButtonProps = ButtonProps;
