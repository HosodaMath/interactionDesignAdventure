/**
 * @property classList 配列で定義したクラス名
 */
export type sectionProps = {
  classList: string[];
};

/**
 * @property classList 配列で定義したクラス名
 */
export type DivisionProps = sectionProps;

export type ImageProps = {
  classList: string[];
  imageURL: string;
};
